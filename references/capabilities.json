{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Measured, not asserted: test/behavior.test.mjs runs a probe per feature against the engine in testedEngine and fails when a recorded value disagrees. Two values here were stale falses until that probe existed.",
  "testedEngine": "@markup-carve/carve@0.1.7",
  "policy": "Prefer released-engine behavior. A spec-only construct must not be authored unless the target explicitly supports it.",
  "features": {
    "trailing_comment": {
      "syntax": "%%",
      "spec": true,
      "inTestedEngine": true
    },
    "fenced_comment": {
      "syntax": "%%%",
      "spec": true,
      "inTestedEngine": true
    },
    "delimited_comment": {
      "syntax": "{% comment %}",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "Shipped in 0.1.4. Older engines need %% instead."
    },
    "container_exact_closer": {
      "syntax": "equal-width nested ::: fences",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "Shipped in 0.1.4: an equal-width nested container nests. An equal-width CODE fence still closes early."
    },
    "admonition_landmark_name": {
      "syntax": "::: note with an optional quoted title",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "Shipped in 0.1.6: untitled admonitions use aria-label; titled admonitions use aria-labelledby pointing to the visible title."
    },
    "raw_html": {
      "syntax": "`x`{=html} or ```=html",
      "spec": true,
      "inTestedEngine": true,
      "renderer": "html only"
    },
    "tier3_renderers": {
      "syntax": "mermaid, chart, rendered math, host directives",
      "spec": true,
      "inTestedEngine": "host-dependent"
    },
    "vertical_cell_alignment": {
      "syntax": "|=<^ and |=>v, a second table alignment axis",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "Shipped in 0.1.5. Was prose-only guidance saying 0.1.4 left the markers as literal cell text, which stayed in the skill for a release after it stopped being true."
    },
    "nested_bare_emphasis": {
      "syntax": "_*x*_, one bare mark directly inside another",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "PART 3's word-boundary guard exempts a delimiter that opens directly after a `_` or `/` which itself opens a span that closes. `_*x*_` nests; `_*x* q` stays literal."
    },
    "fence_opener_fallback_lint": {
      "syntax": "carve lint reporting a fence opener whose info string is not valid",
      "spec": true,
      "inTestedEngine": false,
      "guidance": "The spec's validation rule table names `fence-opener-fallback`; 0.1.7 does not emit it, so ```js {.diff} degrades to an inline code span with no finding. Check the render until this flips."
    },
    "doubled_arrows": {
      "syntax": "==> <== <=> smart-punctuation arrows",
      "spec": true,
      "inTestedEngine": true,
      "guidance": "Shipped in 0.1.5, and `=>` STOPPED converting in the same release. Write ->, <-, <->, ==>, <== or <=>; `=>` is literal text now."
    }
  }
}
